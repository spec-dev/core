import { ExternalTrace } from '../types'

async function storeTraces(
    tracesPromise: Promise<ExternalTrace[]>,
    transactions: Transaction[],
): Promise<Trace[]> {
    
}

export default storeTraces